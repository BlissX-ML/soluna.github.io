---
titleEn: 'Browser Back and Refresh'
titleCh: '浏览器的回退和刷新'
tags: ['browser']
---


| 操作 | 浏览器行为 | 是否重新发请求？ | 是否重新提交表单？ |
| --- | --- | --- | --- |
| **刷新（F5）** | 重新加载当前页面 | ✅ 会重新发请求 | ⚠️ 如果是POST请求，会提示“是否重新提交表单” |
| **回退（←）** | 回到上一个历史记录 | ✅ 可能会从缓存加载，也可能重新发请求 | ⚠️ 如果上一页是POST结果页，会提示“表单重复提交” |

