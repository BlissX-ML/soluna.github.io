---
titleEn: 'Document Flow'
titleCh: '文档流的概念'
tags: ['Document Flow']
---


1. **正常文档流 (Normal Flow)**：
    - 元素按照标准规则依次排布（块级元素自上而下，内联元素从左到右）。
    
2. **脱离文档流 (Out of Flow)**：
    - 元素不再按照正常文档流的规则参与排版，它的几何位置由 **独立的定位机制** 决定。
    - 例如：`position: absolute`、`position: fixed`